<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.SinglePlaylistActivity"
    android:background="@color/background_purple">

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/add_song_to_playlist"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="20dp"
        android:layout_marginStart="20dp"
        android:layout_alignTop="@+id/playlist_name"
        android:layout_toEndOf="@+id/playlist_name"
        android:background="@drawable/awesome_round_button" />

    <TextView
        android:id="@+id/playlist_name"
        android:layout_width="263dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        android:fontFamily="@font/futura_pt_book"
        android:textColor="@color/white"
        android:textSize="@android:dimen/app_icon_size" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/playlistsRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/playlist_name"
        android:layout_marginTop="10dp" />



</RelativeLayout>